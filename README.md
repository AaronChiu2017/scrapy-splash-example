# scrapy-splash
scrapy example with spash

## pre-requisites
- docker
- docker-compose

## install
```sh
$ git clone https://github.com/verzola/scrapy-splash.git
$ cd scrapy-splash
$ docker-compose up -d
```
